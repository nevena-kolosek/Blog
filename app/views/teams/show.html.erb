<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>

<%= link_to 'Edit', edit_team_path(@team) %> 


<h1>All users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th></th>
      <th>Role</th>
      <th>Added</th>
      <th>Teams</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to 'Invite', invite_team_path(@user) %></td>
		    <td>
          <%= form_for(user) do |f| %>
    	  		<p>
    	    		<%= f.text_field :role %>
    	  		</p>
	  	    <% end %>
	       </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', teams_path %>




